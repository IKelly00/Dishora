import './bootstrap'; // keep existing bootstrap setup

// Import Bootstrap JS (dropdowns, modals, tooltips, etc.)
import * as bootstrap from 'bootstrap';

/*
  Add custom scripts here
*/
import.meta.glob([
  '../assets/img/**',
  // '../assets/json/**',
  '../assets/vendor/fonts/**'
]);
